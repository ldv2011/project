import React, { useState, useEffect } from "react";
import { 
  ShieldCheck, 
  Lock, 
  Key, 
  Globe, 
  Cpu, 
  Activity, 
  CheckCircle2, 
  ChevronRight, 
  Menu, 
  X, 
  Terminal, 
  Zap, 
  AlertTriangle, 
  RefreshCw,
  Search,
  Sliders,
  Sparkles,
  Server,
  ArrowUpRight
} from "lucide-react";

// --- AI API Helper Function ---
const apiKey = ""; // Set by runtime environment

async function fetchGeminiSecurityAnalysis(promptText) {
  const systemPrompt = `You are Securify AI, an elite cybersecurity assistant. Analyze the given infrastructure/security prompt and output a concise, structured analysis in JSON with these keys: 
  "threatLevel" ("Low", "Medium", "High", "Critical"), 
  "vulnerabilitiesFound" (number), 
  "summary" (1-2 sentences), 
  "recommendations" (array of 3 short actionable string tips).`;

  let retries = 0;
  const delays = [1000, 2000, 4000, 8000, 16000];

  while (retries < 5) {
    try {
      const response = await fetch(
        `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-09-2025:generateContent?key=${apiKey}`,
        {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({
            contents: [{ parts: [{ text: promptText }] }],
            systemInstruction: { parts: [{ text: systemPrompt }] },
            generationConfig: {
              responseMimeType: "application/json",
              responseSchema: {
                type: "OBJECT",
                properties: {
                  threatLevel: { type: "STRING" },
                  vulnerabilitiesFound: { type: "INTEGER" },
                  summary: { type: "STRING" },
                  recommendations: {
                    type: "ARRAY",
                    items: { type: "STRING" }
                  }
                },
                required: ["threatLevel", "vulnerabilitiesFound", "summary", "recommendations"]
              }
            }
          })
        }
      );

      if (!response.ok) {
        throw new Error(`HTTP error! Status: ${response.status}`);
      }

      const data = await response.json();
      const text = data.candidates?.[0]?.content?.parts?.[0]?.text;
      if (text) {
        return JSON.parse(text);
      }
      throw new Error("Invalid response structure");
    } catch (err) {
      retries++;
      if (retries >= 5) {
        throw err;
      }
      await new Promise((res) => setTimeout(res, delays[retries - 1]));
    }
  }
}

// --- Logo Component ---
const Logo = () => (
  <svg viewBox="0 0 256 256" className="h-5 w-5 fill-current" xmlns="http://www.w3.org/2000/svg">
    <path
      d="M 128 192 L 128 256 L 64.5 256 L 32 223 L 0 192 L 0 128 L 64 128 Z M 256 192 L 256 256 L 192.5 256 L 160 223 L 128 192 L 128 128 L 192 128 Z M 128 64 L 128 128 L 64.5 128 L 32 95 L 0 64 L 0 0 L 64 0 Z M 256 64 L 256 128 L 192.5 128 L 160 95 L 128 64 L 128 0 L 192 0 Z"
    />
  </svg>
);

// --- Navbar Component ---
const Navbar = ({ onOpenScanModal }) => {
  const [mobileMenuOpen, setMobileMenuOpen] = useState(false);

  return (
    <header className="relative z-50">
      <nav className="flex items-center justify-between gap-4">
        {/* Brand */}
        <div className="flex items-center gap-2 bg-neutral-900/90 border border-neutral-800/80 backdrop-blur rounded-full pl-4 pr-6 py-3 shadow-lg">
          <Logo />
          <span className="text-white text-sm font-medium tracking-tight">securify</span>
        </div>

        {/* Desktop Links */}
        <div className="hidden md:flex items-center gap-1 bg-neutral-900/90 border border-neutral-800/80 backdrop-blur rounded-full px-3 py-2 shadow-lg">
          <a href="#platform" className="text-neutral-300 hover:text-white transition-colors text-sm px-5 py-2 rounded-full hover:bg-neutral-800/60">
            platform
          </a>
          <a href="#features" className="text-neutral-300 hover:text-white transition-colors text-sm px-5 py-2 rounded-full hover:bg-neutral-800/60">
            solutions
          </a>
          <a href="#ai-analyzer" className="text-neutral-300 hover:text-white transition-colors text-sm px-5 py-2 rounded-full hover:bg-neutral-800/60">
            ai scanner
          </a>
          <a href="#pricing" className="text-neutral-300 hover:text-white transition-colors text-sm px-5 py-2 rounded-full hover:bg-neutral-800/60">
            pricing
          </a>
        </div>

        {/* Actions */}
        <div className="flex items-center gap-3">
          <button 
            onClick={onOpenScanModal}
            className="hidden sm:inline-flex bg-white text-black text-sm font-medium rounded-full px-6 py-3 hover:bg-neutral-200 transition-colors shadow-md hover:shadow-white/10"
          >
            get started
          </button>

          {/* Mobile menu toggle */}
          <button 
            onClick={() => setMobileMenuOpen(!mobileMenuOpen)}
            className="md:hidden bg-neutral-900 border border-neutral-800 p-3 rounded-full text-white"
          >
            {mobileMenuOpen ? <X className="w-5 h-5" /> : <Menu className="w-5 h-5" />}
          </button>
        </div>
      </nav>

      {/* Mobile Menu Dropdown */}
      {mobileMenuOpen && (
        <div className="md:hidden absolute top-16 left-0 right-0 bg-neutral-950 border border-neutral-800 rounded-3xl p-6 shadow-2xl flex flex-col gap-4 mt-2">
          <a 
            href="#platform" 
            onClick={() => setMobileMenuOpen(false)} 
            className="text-neutral-300 hover:text-white text-lg py-2 border-b border-neutral-900"
          >
            platform
          </a>
          <a 
            href="#features" 
            onClick={() => setMobileMenuOpen(false)} 
            className="text-neutral-300 hover:text-white text-lg py-2 border-b border-neutral-900"
          >
            solutions
          </a>
          <a 
            href="#ai-analyzer" 
            onClick={() => setMobileMenuOpen(false)} 
            className="text-neutral-300 hover:text-white text-lg py-2 border-b border-neutral-900"
          >
            ai scanner
          </a>
          <a 
            href="#pricing" 
            onClick={() => setMobileMenuOpen(false)} 
            className="text-neutral-300 hover:text-white text-lg py-2 border-b border-neutral-900"
          >
            pricing
          </a>
          <button 
            onClick={() => {
              setMobileMenuOpen(false);
              onOpenScanModal();
            }}
            className="w-full bg-white text-black font-medium py-3 rounded-full mt-2"
          >
            get started
          </button>
        </div>
      )}
    </header>
  );
};

// --- Interactive AI Vulnerability Scanner Section ---
const SecurityAIScanner = () => {
  const [inputPrompt, setInputPrompt] = useState("Scan Kubernetes cluster staging namespace with public endpoint enabled.");
  const [loading, setLoading] = useState(false);
  const [result, setResult] = useState(null);
  const [error, setError] = useState("");

  const handleScan = async (e) => {
    e?.preventDefault();
    if (!inputPrompt.trim()) return;

    setLoading(true);
    setError("");
    setResult(null);

    try {
      const data = await fetchGeminiSecurityAnalysis(inputPrompt);
      setResult(data);
    } catch (err) {
      setError("Failed to run AI Security Audit. Please try again.");
    } finally {
      setLoading(false);
    }
  };

  return (
    <section id="ai-analyzer" className="py-24 px-6 md:px-12 max-w-7xl mx-auto">
      <div className="text-center max-w-3xl mx-auto mb-12">
        <div className="inline-flex items-center gap-2 px-4 py-1.5 rounded-full border border-neutral-800 bg-neutral-900/60 text-xs font-mono uppercase tracking-wider text-emerald-400 mb-4">
          <Sparkles className="w-3.5 h-3.5" /> Powered by Gemini 2.5 Intelligence
        </div>
        <h2 className="text-3xl md:text-5xl font-medium tracking-tight text-white mb-4">
          Instant Zero-Trust AI Security Analysis
        </h2>
        <p className="text-neutral-400 text-base md:text-lg">
          Simulate an infrastructure assessment in seconds. Input your setup or deployment configuration below.
        </p>
      </div>

      <div className="bg-neutral-900/80 border border-neutral-800 rounded-3xl p-6 md:p-10 shadow-2xl backdrop-blur">
        <form onSubmit={handleScan} className="flex flex-col md:flex-row gap-4 mb-8">
          <div className="relative flex-1">
            <Terminal className="absolute left-4 top-1/2 -translate-y-1/2 w-5 h-5 text-neutral-500" />
            <input
              type="text"
              value={inputPrompt}
              onChange={(e) => setInputPrompt(e.target.value)}
              placeholder="e.g. AWS S3 bucket public access with multi-region replication enabled"
              className="w-full bg-neutral-950 border border-neutral-800 rounded-2xl pl-12 pr-4 py-4 text-sm md:text-base text-white placeholder-neutral-500 focus:outline-none focus:border-white transition"
            />
          </div>
          <button
            type="submit"
            disabled={loading}
            className="bg-white hover:bg-neutral-200 text-black font-medium px-8 py-4 rounded-2xl text-sm transition flex items-center justify-center gap-2 disabled:opacity-50"
          >
            {loading ? (
              <>
                <RefreshCw className="w-4 h-4 animate-spin" />
                Analyzing Architecture...
              </>
            ) : (
              <>
                <Zap className="w-4 h-4 fill-current" />
                Run AI Scan
              </>
            )}
          </button>
        </form>

        {/* Example Quick Prompts */}
        <div className="flex flex-wrap items-center gap-2 mb-8 text-xs text-neutral-400">
          <span className="font-mono text-neutral-500">Quick Scans:</span>
          {[
            "Node.js API with JWT authentication stored in localStorage",
            "Multi-cloud PostgreSQL database with SSL disabled",
            "Docker container running with root privileges on AWS ECS"
          ].map((prompt, idx) => (
            <button
              key={idx}
              onClick={() => {
                setInputPrompt(prompt);
              }}
              className="px-3 py-1.5 rounded-lg bg-neutral-950 hover:bg-neutral-800 border border-neutral-800 text-neutral-300 transition text-left"
            >
              "{prompt.slice(0, 38)}..."
            </button>
          ))}
        </div>

        {error && (
          <div className="p-4 rounded-xl bg-red-950/40 border border-red-800/60 text-red-300 text-sm flex items-center gap-3">
            <AlertTriangle className="w-5 h-5 text-red-400 flex-shrink-0" />
            {error}
          </div>
        )}

        {/* Results Display */}
        {result && (
          <div className="mt-6 border border-neutral-800 bg-neutral-950/90 rounded-2xl p-6 md:p-8 animate-fadeIn">
            <div className="flex flex-col sm:flex-row items-start sm:items-center justify-between gap-4 pb-6 border-b border-neutral-800">
              <div>
                <span className="text-xs uppercase tracking-wider text-neutral-500 font-mono">Assessment Target</span>
                <h4 className="text-white font-medium text-lg mt-1">{inputPrompt}</h4>
              </div>
              <div className="flex items-center gap-3">
                <div className="text-right">
                  <div className="text-xs text-neutral-400 font-mono uppercase">Threat Level</div>
                  <span className={`text-sm font-semibold uppercase px-3 py-1 rounded-full inline-block mt-1 ${
                    result.threatLevel?.toLowerCase() === 'critical' || result.threatLevel?.toLowerCase() === 'high'
                      ? 'bg-red-500/10 text-red-400 border border-red-500/30'
                      : result.threatLevel?.toLowerCase() === 'medium'
                      ? 'bg-amber-500/10 text-amber-400 border border-amber-500/30'
                      : 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/30'
                  }`}>
                    {result.threatLevel}
                  </span>
                </div>
                <div className="text-right border-l border-neutral-800 pl-4">
                  <div className="text-xs text-neutral-400 font-mono uppercase">Risks Detected</div>
                  <span className="text-2xl font-bold text-white mt-1 block">
                    {result.vulnerabilitiesFound}
                  </span>
                </div>
              </div>
            </div>

            <div className="py-6 border-b border-neutral-800">
              <h5 className="text-xs font-mono uppercase text-neutral-400 mb-2">Executive Summary</h5>
              <p className="text-neutral-200 text-sm leading-relaxed">{result.summary}</p>
            </div>

            <div className="pt-6">
              <h5 className="text-xs font-mono uppercase text-neutral-400 mb-4">Recommended Remediation</h5>
              <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
                {result.recommendations?.map((rec, i) => (
                  <div key={i} className="p-4 rounded-xl bg-neutral-900 border border-neutral-800 flex items-start gap-3">
                    <CheckCircle2 className="w-5 h-5 text-emerald-400 flex-shrink-0 mt-0.5" />
                    <span className="text-neutral-300 text-xs leading-normal">{rec}</span>
                  </div>
                ))}
              </div>
            </div>
          </div>
        )}
      </div>
    </section>
  );
};

// --- Live Threat Radar Component ---
const LiveThreatRadar = () => {
  const [activeTab, setActiveTab] = useState('threats');
  const [logs, setLogs] = useState([
    { id: 1, time: "14:32:01", type: "Encrypted Token", region: "US-East (N. Virginia)", status: "SECURE", ip: "192.168.1.104" },
    { id: 2, time: "14:31:58", type: "Zero-Knowledge Proof", region: "EU-Central (Frankfurt)", status: "VERIFIED", ip: "10.0.42.11" },
    { id: 3, time: "14:31:45", type: "Key Rotation", region: "AP-East (Tokyo)", status: "COMPLETED", ip: "172.16.0.88" },
    { id: 4, time: "14:31:22", type: "Anomaly Mitigation", region: "SA-East (São Paulo)", status: "BLOCKED", ip: "45.12.99.12" }
  ]);

  useEffect(() => {
    const interval = setInterval(() => {
      const types = ["Quantum Encryption Handshake", "Zero-Knowledge Auth", "API Gateway Scrub", "Key Rotation"];
      const regions = ["US-West (Oregon)", "EU-West (London)", "AP-South (Mumbai)", "US-East (N. Virginia)"];
      const statuses = ["SECURE", "VERIFIED", "COMPLETED", "BLOCKED"];
      
      const newLog = {
        id: Date.now(),
        time: new Date().toLocaleTimeString('en-US', { hour12: false }),
        type: types[Math.floor(Math.random() * types.length)],
        region: regions[Math.floor(Math.random() * regions.length)],
        status: statuses[Math.floor(Math.random() * statuses.length)],
        ip: `${Math.floor(Math.random()*255)}.${Math.floor(Math.random()*255)}.${Math.floor(Math.random()*255)}.${Math.floor(Math.random()*255)}`
      };

      setLogs(prev => [newLog, ...prev.slice(0, 5)]);
    }, 3500);

    return () => clearInterval(interval);
  }, []);

  return (
    <section id="platform" className="py-20 px-6 md:px-10 max-w-7xl mx-auto">
      <div className="grid grid-cols-1 lg:grid-cols-12 gap-8 items-center">
        <div className="lg:col-span-5 space-y-6">
          <div className="inline-flex items-center gap-2 px-3 py-1 rounded-full border border-neutral-800 bg-neutral-900 text-xs text-neutral-400 font-mono">
            <Activity className="w-3.5 h-3.5 text-emerald-400 animate-pulse" />
            Global Threat Grid Active
          </div>
          <h2 className="text-3xl md:text-5xl font-medium tracking-tight text-white leading-tight">
            Autonomous threat detection in real-time.
          </h2>
          <p className="text-neutral-400 text-base leading-relaxed">
            Securify continuously monitors your cloud perimeter, applying military-grade Zero-Knowledge encryption to insulate your mission-critical data payloads.
          </p>

          <div className="space-y-3 pt-2">
            {[
              "End-to-end ZK-SNARK data verification",
              "Sub-millisecond global key distribution",
              "Automated compliance auditing (SOC2, ISO27001, HIPAA)"
            ].map((feature, i) => (
              <div key={i} className="flex items-center gap-3 text-neutral-300 text-sm">
                <div className="w-5 h-5 rounded-full bg-white/10 flex items-center justify-center text-white">
                  ✓
                </div>
                <span>{feature}</span>
              </div>
            ))}
          </div>
        </div>

        {/* Interactive Cyber Terminal Visual */}
        <div className="lg:col-span-7 bg-neutral-950 border border-neutral-800 rounded-3xl p-6 shadow-2xl relative overflow-hidden">
          <div className="flex items-center justify-between pb-4 border-b border-neutral-800">
            <div className="flex items-center gap-2">
              <div className="w-3 h-3 rounded-full bg-red-500/80" />
              <div className="w-3 h-3 rounded-full bg-amber-500/80" />
              <div className="w-3 h-3 rounded-full bg-emerald-500/80" />
              <span className="text-xs font-mono text-neutral-500 ml-2">securify-edge-node://telemetry</span>
            </div>
            <div className="flex gap-2">
              <button 
                onClick={() => setActiveTab('threats')} 
                className={`text-xs font-mono px-3 py-1 rounded-lg transition ${activeTab === 'threats' ? 'bg-neutral-800 text-white' : 'text-neutral-500 hover:text-neutral-300'}`}
              >
                Live Feed
              </button>
              <button 
                onClick={() => setActiveTab('nodes')} 
                className={`text-xs font-mono px-3 py-1 rounded-lg transition ${activeTab === 'nodes' ? 'bg-neutral-800 text-white' : 'text-neutral-500 hover:text-neutral-300'}`}
              >
                Global Nodes
              </button>
            </div>
          </div>

          {activeTab === 'threats' ? (
            <div className="space-y-3 mt-4">
              <div className="grid grid-cols-12 text-[10px] font-mono uppercase text-neutral-500 px-3 pb-1 border-b border-neutral-900">
                <span className="col-span-2">Time</span>
                <span className="col-span-4">Operation</span>
                <span className="col-span-3">Node Region</span>
                <span className="col-span-3 text-right">Status</span>
              </div>
              {logs.map((log) => (
                <div key={log.id} className="grid grid-cols-12 items-center text-xs font-mono px-3 py-2.5 rounded-xl bg-neutral-900/40 hover:bg-neutral-900/80 transition border border-neutral-800/40">
                  <span className="col-span-2 text-neutral-500">{log.time}</span>
                  <span className="col-span-4 text-neutral-200 font-medium truncate">{log.type}</span>
                  <span className="col-span-3 text-neutral-400 text-[11px] truncate">{log.region}</span>
                  <span className="col-span-3 text-right">
                    <span className="inline-block px-2 py-0.5 rounded text-[10px] font-bold bg-emerald-950 text-emerald-400 border border-emerald-800/50">
                      {log.status}
                    </span>
                  </span>
                </div>
              ))}
            </div>
          ) : (
            <div className="py-12 px-4 text-center space-y-4">
              <div className="inline-flex p-4 rounded-2xl bg-neutral-900 border border-neutral-800 text-emerald-400">
                <Globe className="w-8 h-8 animate-pulse" />
              </div>
              <h4 className="text-white font-medium text-lg">142 Global Edge Clusters Online</h4>
              <p className="text-neutral-400 text-xs max-w-sm mx-auto">
                All edge relays operating with &lt;4ms latency across North America, Europe, and Asia-Pacific.
              </p>
              <div className="pt-2 flex justify-center gap-6 text-xs text-neutral-400 font-mono">
                <div>Uptime: <span className="text-emerald-400">99.999%</span></div>
                <div>Avg Latency: <span className="text-emerald-400">3.2ms</span></div>
              </div>
            </div>
          )}

          <div className="mt-4 pt-4 border-t border-neutral-900 flex justify-between items-center text-[11px] text-neutral-500 font-mono">
            <span>Enforcement Engine v4.8</span>
            <span className="text-emerald-400 flex items-center gap-1">
              <span className="w-1.5 h-1.5 rounded-full bg-emerald-400 animate-ping"></span>
              Realtime Shield Enabled
            </span>
          </div>
        </div>
      </div>
    </section>
  );
};

// --- Features Grid ---
const FeaturesGrid = () => {
  const features = [
    {
      icon: <ShieldCheck className="w-6 h-6 text-white" />,
      title: "Zero-Knowledge Architecture",
      description: "Data is mathematically encrypted before leaving your hardware. Even Securify cannot view your raw payloads."
    },
    {
      icon: <Lock className="w-6 h-6 text-white" />,
      title: "Quantum-Resistant Vaults",
      description: "Lattice-based cryptography algorithms built to withstand next-generation quantum computing decryption attempts."
    },
    {
      icon: <Key className="w-6 h-6 text-white" />,
      title: "Granular Access Governance",
      description: "Define real-time permission policies based on user context, IP risk score, biometric verification, and location."
    },
    {
      icon: <Cpu className="w-6 h-6 text-white" />,
      title: "Autonomous Threat Scrubbing",
      description: "AI-driven edge security automatically isolates suspicious traffic bursts without disrupting legitimate operations."
    }
  ];

  return (
    <section id="features" className="py-24 px-6 md:px-10 max-w-7xl mx-auto border-t border-neutral-900">
      <div className="flex flex-col md:flex-row md:items-end justify-between mb-16 gap-6">
        <div>
          <span className="text-xs font-mono uppercase text-neutral-500 tracking-wider">Engineered for absolute privacy</span>
          <h2 className="text-3xl md:text-5xl font-medium tracking-tight text-white mt-2">
            Built for security-first enterprises.
          </h2>
        </div>
        <p className="text-neutral-400 text-sm md:text-base max-w-md">
          Integrate our lightweight SDKs into your existing tech stack in under ten minutes with zero code rewrite.
        </p>
      </div>

      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
        {features.map((f, i) => (
          <div 
            key={i} 
            className="p-8 rounded-3xl bg-neutral-900/40 border border-neutral-800/80 hover:border-neutral-700 hover:bg-neutral-900/80 transition group"
          >
            <div className="w-12 h-12 rounded-2xl bg-neutral-800 flex items-center justify-center mb-6 group-hover:scale-110 transition">
              {f.icon}
            </div>
            <h3 className="text-xl font-medium text-white mb-3">{f.title}</h3>
            <p className="text-neutral-400 text-sm leading-relaxed">{f.description}</p>
          </div>
        ))}
      </div>
    </section>
  );
};

// --- Pricing Section ---
const PricingSection = ({ onOpenScanModal }) => {
  const plans = [
    {
      name: "Developer",
      price: "$0",
      period: "forever free",
      description: "For startups and indie developers protecting core infrastructure.",
      features: [
        "Up to 100 GB protected data/mo",
        "Community support & Discord access",
        "Standard AES-256 GCM encryption",
        "Basic threat audit logs"
      ],
      popular: false,
      cta: "Get Started Free"
    },
    {
      name: "Scale Enterprise",
      price: "$499",
      period: "per month",
      description: "For fast-growing companies demanding zero-trust and compliance.",
      features: [
        "Unlimited data volume encryption",
        "Zero-Knowledge (ZK) Proof Vaults",
        "SOC2 & HIPAA Compliance reports",
        "24/7 Priority Security Engineer On-call",
        "Custom key management (BYOK)"
      ],
      popular: true,
      cta: "Start 14-Day Trial"
    },
    {
      name: "Custom Defense",
      price: "Custom",
      period: "annual billing",
      description: "Dedicated air-gapped infrastructure for mission-critical deployments.",
      features: [
        "Air-gapped on-premise installation",
        "Custom SLA guarantees (99.999%)",
        "Dedicated security research team",
        "Quantum-resistant lattice algorithms"
      ],
      popular: false,
      cta: "Contact Security Team"
    }
  ];

  return (
    <section id="pricing" className="py-24 px-6 md:px-10 max-w-7xl mx-auto border-t border-neutral-900">
      <div className="text-center max-w-3xl mx-auto mb-16">
        <span className="text-xs font-mono uppercase text-neutral-500 tracking-wider">Transparent Protection</span>
        <h2 className="text-3xl md:text-5xl font-medium tracking-tight text-white mt-2 mb-4">
          Simple, predictable enterprise pricing.
        </h2>
        <p className="text-neutral-400 text-base">
          Scale your security posture seamlessly as your data footprint expands globally.
        </p>
      </div>

      <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
        {plans.map((p, i) => (
          <div 
            key={i} 
            className={`rounded-3xl p-8 flex flex-col justify-between relative ${
              p.popular 
                ? 'bg-neutral-900 border-2 border-white/80 shadow-2xl shadow-white/5' 
                : 'bg-neutral-950 border border-neutral-800'
            }`}
          >
            {p.popular && (
              <span className="absolute -top-3.5 left-1/2 -translate-x-1/2 bg-white text-black text-[11px] font-bold px-4 py-1 rounded-full uppercase tracking-wider">
                Most Popular
              </span>
            )}

            <div>
              <div className="text-lg font-medium text-white mb-2">{p.name}</div>
              <div className="flex items-baseline gap-1 my-4">
                <span className="text-4xl md:text-5xl font-bold text-white tracking-tight">{p.price}</span>
                <span className="text-xs text-neutral-400 font-mono">{p.period}</span>
              </div>
              <p className="text-neutral-400 text-xs mb-8 leading-relaxed">{p.description}</p>

              <div className="space-y-3 mb-8 border-t border-neutral-800/80 pt-6">
                {p.features.map((feat, idx) => (
                  <div key={idx} className="flex items-center gap-3 text-xs text-neutral-300">
                    <CheckCircle2 className="w-4 h-4 text-emerald-400 flex-shrink-0" />
                    <span>{feat}</span>
                  </div>
                ))}
              </div>
            </div>

            <button
              onClick={onOpenScanModal}
              className={`w-full py-3.5 rounded-full text-sm font-medium transition ${
                p.popular
                  ? 'bg-white text-black hover:bg-neutral-200'
                  : 'bg-neutral-900 hover:bg-neutral-800 text-white border border-neutral-800'
              }`}
            >
              {p.cta}
            </button>
          </div>
        ))}
      </div>
    </section>
  );
};

// --- Footer ---
const Footer = () => (
  <footer className="border-t border-neutral-900 bg-black py-16 px-6 md:px-10">
    <div className="max-w-7xl mx-auto flex flex-col md:flex-row justify-between items-start md:items-center gap-8">
      <div className="flex items-center gap-2">
        <Logo />
        <span className="text-white text-lg font-medium tracking-tight">securify</span>
      </div>

      <div className="flex flex-wrap gap-8 text-xs text-neutral-400 font-mono">
        <a href="#platform" className="hover:text-white transition">Platform</a>
        <a href="#features" className="hover:text-white transition">Solutions</a>
        <a href="#ai-analyzer" className="hover:text-white transition">AI Scanner</a>
        <a href="#pricing" className="hover:text-white transition">Pricing</a>
        <a href="#" className="hover:text-white transition">Privacy Policy</a>
        <a href="#" className="hover:text-white transition">Terms of Service</a>
      </div>

      <div className="text-xs text-neutral-600 font-mono">
        © {new Date().getFullYear()} Securify Security Inc. All rights reserved.
      </div>
    </div>
  </footer>
);

// --- Modal Get Started / Scan Dialog ---
const Modal = ({ isOpen, onClose }) => {
  if (!isOpen) return null;

  return (
    <div className="fixed inset-0 z-50 flex items-center justify-center p-4 bg-black/80 backdrop-blur-sm animate-fadeIn">
      <div className="bg-neutral-900 border border-neutral-800 rounded-3xl p-8 max-w-md w-full relative shadow-2xl">
        <button 
          onClick={onClose} 
          className="absolute top-6 right-6 text-neutral-400 hover:text-white"
        >
          <X className="w-5 h-5" />
        </button>

        <div className="flex items-center gap-2 mb-4">
          <Logo />
          <span className="text-white font-medium">Securify Cloud</span>
        </div>

        <h3 className="text-2xl font-medium text-white mb-2">Initialize Security Vault</h3>
        <p className="text-neutral-400 text-sm mb-6">
          Create an enterprise account or connect your cloud provider to start safeguarding your data in minutes.
        </p>

        <form className="space-y-4" onSubmit={(e) => { e.preventDefault(); onClose(); }}>
          <div>
            <label className="block text-xs font-mono text-neutral-400 mb-1">Work Email</label>
            <input 
              type="email" 
              required
              placeholder="alex@company.com" 
              className="w-full bg-neutral-950 border border-neutral-800 rounded-xl px-4 py-3 text-sm text-white placeholder-neutral-600 focus:outline-none focus:border-white"
            />
          </div>

          <div>
            <label className="block text-xs font-mono text-neutral-400 mb-1">Infrastructure Provider</label>
            <select className="w-full bg-neutral-950 border border-neutral-800 rounded-xl px-4 py-3 text-sm text-neutral-300 focus:outline-none focus:border-white">
              <option>Amazon Web Services (AWS)</option>
              <option>Google Cloud Platform (GCP)</option>
              <option>Microsoft Azure</option>
              <option>Kubernetes / On-Premise</option>
            </select>
          </div>

          <button 
            type="submit" 
            className="w-full bg-white text-black font-medium py-3.5 rounded-xl hover:bg-neutral-200 transition mt-2 text-sm"
          >
            Create Free Account
          </button>

          <p className="text-[11px] text-neutral-500 text-center mt-4">
            No credit card required. Includes 14 days of full scale Enterprise Features.
          </p>
        </form>
      </div>
    </div>
  );
};

// --- Main App Component ---
export default function App() {
  const [modalOpen, setModalOpen] = useState(false);

  return (
    <div className="bg-black text-white min-h-screen selection:bg-white selection:text-black font-sans antialiased">
      <style>{`
        @import url('https://fonts.googleapis.com/css2?family=Readex+Pro:wght@300;400;500;600;700&display=swap');

        body {
          font-family: 'Readex Pro', system-ui, -apple-system, sans-serif;
          background: #000;
          color: #fff;
        }

        .hero-title {
          letter-spacing: -0.04em;
          line-height: 0.95;
        }

        @keyframes fadeIn {
          from { opacity: 0; transform: translateY(6px); }
          to { opacity: 1; transform: translateY(0); }
        }

        .animate-fadeIn {
          animation: fadeIn 0.3s cubic-bezier(0.16, 1, 0.3, 1) forwards;
        }
      `}</style>

      {/* --- Original User Hero Section --- */}
      <section className="relative h-screen w-full overflow-hidden bg-black">
        <video
          className="absolute inset-0 w-full h-full object-cover opacity-80"
          autoPlay
          loop
          muted
          playsInline
          src="https://d8j0ntlcm91z4.cloudfront.net/user_38xzZboKViGWJOttwIXH07lWA1P/hf_20260418_063509_7d167302-4fd4-480b-8260-18ab572333d4.mp4"
        />

        <div className="absolute top-0 left-0 right-0 z-20 px-6 md:px-10 pt-6">
          <Navbar onOpenScanModal={() => setModalOpen(true)} />
        </div>

        <div className="relative h-full w-full pointer-events-none select-none">
          <h1 className="hero-title absolute text-white font-medium text-[14vw] md:text-[13vw] left-4 md:left-10 top-[18%]">
            protect
          </h1>
          <h1 className="hero-title absolute text-white font-medium text-[14vw] md:text-[13vw] right-4 md:right-10 top-[38%]">
            your
          </h1>
          <h1 className="hero-title absolute text-white font-medium text-[14vw] md:text-[13vw] left-[18%] md:left-[28%] top-[58%]">
            data
          </h1>

          <p className="absolute left-6 md:left-10 top-[46%] max-w-[240px] text-[15px] leading-snug text-white/90 font-light drop-shadow">
            we can guarding your data with utmost care, empowering you with privacy everywhere
          </p>

          <div className="absolute right-6 md:right-24 top-[14%]">
            <div className="flex items-center gap-3 justify-end">
              <div className="hidden md:block h-px w-24 bg-white/40 rotate-[20deg]" />
              <span className="text-4xl md:text-5xl font-medium tracking-tight">+65k</span>
            </div>
            <div className="text-xs md:text-sm text-white/70 mt-1 text-right">startups use</div>
          </div>

          <div className="absolute left-6 md:left-20 bottom-20 md:bottom-24">
            <div className="flex items-center gap-3">
              <span className="text-4xl md:text-5xl font-medium tracking-tight">+1.5b</span>
              <div className="hidden md:block h-px w-24 bg-white/40 rotate-[-20deg]" />
            </div>
            <div className="text-xs md:text-sm text-white/70 mt-1">gb data was protected</div>
          </div>

          <div className="absolute right-6 md:right-20 bottom-16 md:bottom-20">
            <div className="flex items-center gap-3 justify-end">
              <div className="hidden md:block h-px w-24 bg-white/40 rotate-[-20deg]" />
              <span className="text-4xl md:text-5xl font-medium tracking-tight">+300k</span>
            </div>
            <div className="text-xs md:text-sm text-white/70 mt-1 text-right">downloads</div>
          </div>
        </div>

        {/* Bottom subtle gradient overlay */}
        <div className="pointer-events-none absolute bottom-0 left-0 right-0 h-48 bg-gradient-to-b from-transparent to-black" />
      </section>

      {/* --- Additional Platform Experience Sections --- */}
      <LiveThreatRadar />
      <SecurityAIScanner />
      <FeaturesGrid />
      <PricingSection onOpenScanModal={() => setModalOpen(true)} />

      {/* Footer */}
      <Footer />

      {/* Interactive Modal */}
      <Modal isOpen={modalOpen} onClose={() => setModalOpen(false)} />
    </div>
  );
}